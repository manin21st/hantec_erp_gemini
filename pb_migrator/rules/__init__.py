# -*- coding: utf-8 -*-
"""
마이그레이션 규칙 패키지

이 패키지는 PBMigrator의 개별 마이그레이션 규칙들을 모듈 형태로 포함합니다.
각 파일은 특정 마이그레이션 패턴(예: P-01, P-02)을 구현하며,
엔진에 의해 동적으로 로드되어 사용됩니다.

각 규칙 모듈은 반드시 다음을 포함해야 합니다:
- `apply(code, **kwargs)` 함수: 변환을 수행하고, 결과를 담은 튜플 `(transformed_code, report)`을 반환합니다.
"""
